describe('routes', () => {
 
  it('Get likes', async () => {
    const likes = { 1: '5' };
    expect(true)
  });

  it('Get more likes', async () => {
    const likes = { 1: '5' };
    expect(likes).toEqual({ 1: '5' })
  });

  it('should add numbers', async () => {
    const x = 1, y = 2
    const result = x + y
    expect(true)
  })
});